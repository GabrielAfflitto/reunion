class Activity
  attr_reader :name, :participants, :key, :value
  def initialize(name)
    @name = name
    @participants = {}
    @key = key
    @value = value
  end

  def add_participants
    participants = name
  end

  def total_cost
    participants.value
  end
end
